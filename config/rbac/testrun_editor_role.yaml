# This rule is not used by the project bubo itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the bubo.test.rwdf.no.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: bubo
    app.kubernetes.io/managed-by: kustomize
  name: testrun-editor-role
rules:
- apiGroups:
  - bubo.test.rwdf.no
  resources:
  - testruns
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - bubo.test.rwdf.no
  resources:
  - testruns/status
  verbs:
  - get
